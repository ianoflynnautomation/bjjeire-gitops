apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: github-app-secret
  namespace: actions-runner-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault-store
  refreshInterval: 1h
  target:
    name: github-app-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        github_token: "{{ .github_pat_token }}"
  data:
    - secretKey: github_pat_token
      remoteRef:
        key: gh-flux-aks-token


# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: github-app-secret 
#   namespace: actions-runner-system
#   labels:
#     app.kubernetes.io/name: github-app-secret
#     app.kubernetes.io/part-of: gha-runner-scale-set
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: azure-keyvault-store
#     kind: SecretStore
#   target:
#     name: github-app-secret
#     creationPolicy: Owner 
#     template:
#       type: Opaque
#       data:
#         github_token: "{{ .github_pat_token }}"
#   data:
#     - secretKey: github_pat_token
#       remoteRef:
#         key: gh-flux-aks-token