---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - cronjob.yaml

configMapGenerator:
  - name: pr-env-janitor-scripts
    namespace: pr-env-janitor
    files:
      - janitor.sh
