---
bjj-mongodb:
  mongodb:
    persistence:
      enabled: false
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 256Mi
  secrets:
    mongodbRootPassword:
      enabled: true
      create: false
      name: bjj-mongodb-root-password
      key: mongodb-password

bjj-api:
  api:
    image:
      repository: ghcr.io/ianoflynnautomation/bjjeire-api
      pullPolicy: IfNotPresent
    imagePullSecrets:
      - name: ghcr-pull-secret
    replicaCount: 1
    jvmOpts: "-XX:MaxRAMPercentage=75.0 -XX:InitialRAMPercentage=50.0"
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/path: "/metrics"
      prometheus.io/port: "8080"
    env:
      RATE_LIMIT_ENABLED: "true"
      RATE_LIMIT_PERMIT_LIMIT: "30"
      RATE_LIMIT_WINDOW_SECONDS: "10"
      RATE_LIMIT_REJECTION_STATUS_CODE: "429"
      READ_ONLY_MODE_ENABLED: "false"
      FEATURE_BJJ_EVENTS: "true"
      FEATURE_GYMS: "true"
      FEATURE_STORES: "true"
      FEATURE_COMPETITIONS: "true"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://opentelemetry-collector.observability.svc.cluster.local:4318"
      OTEL_EXPORTER_OTLP_PROTOCOL: "http/protobuf"
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 256Mi
    autoscaling:
      enabled: false
  secrets:
    mongodbRootPassword:
      enabled: true
      create: false
      name: bjj-mongodb-root-password
      key: mongodb-password
  ingress:
    enabled: false

bjj-frontend:
  frontend:
    image:
      repository: ghcr.io/ianoflynnautomation/bjjeire-frontend
      pullPolicy: IfNotPresent
    imagePullSecrets:
      - name: ghcr-pull-secret
    replicaCount: 1
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 101
      fsGroup: 101
      seccompProfile:
        type: RuntimeDefault
      sysctls:
        - name: net.ipv4.ip_unprivileged_port_start
          value: "0"
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/path: "/metrics"
      prometheus.io/port: "80"
    env:
      NODE_ENV: production
      PORT: "80"
    resources:
      limits:
        cpu: 300m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi
    autoscaling:
      enabled: false
    waitForApi:
      enabled: true
  ingress:
    enabled: false

seeder:
  enabled: true
  force: true
  environment: Development
  dataset: "test"
  image:
    repository: ghcr.io/ianoflynnautomation/bjjeire-seeder
    pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-pull-secret
  hookPolicy: "post-install,post-upgrade"
  mongodb:
    host: bjj-mongodb
    port: 27017
    user: admin
    database: Mongodb
    secretName: bjj-mongodb-root-password
    secretKey: mongodb-password
